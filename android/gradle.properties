# org.gradle.jvmargs=-Xmx3g -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m -XX:+UseG1GC
org.gradle.jvmargs=-Xmx3g -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m -XX:+UseG1GC
android.useAndroidX=true
org.gradle.daemon=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false

# Kotlin incremental compilation crashes on Windows when the project (D:)
# and the Gradle/pub cache (C:) are on different drive roots.
kotlin.incremental=false
kotlin.incremental.useClasspathSnapshot=false

